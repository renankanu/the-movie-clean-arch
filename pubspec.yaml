name: the_movie_clean_arch
description: KN Movie App.
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  equatable: ^2.0.5
  dartz: ^0.10.1
  dio: ^4.0.6
  get: ^4.6.5
  cached_network_image: ^3.2.2
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mockito: ^5.3.1
  build_runner: ^2.2.1

flutter:
  uses-material-design: true